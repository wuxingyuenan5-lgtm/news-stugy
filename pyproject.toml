[project]
name = "news-study"
version = "0.2.0"
description = "Cross-asset news collection, event fusion, and daily report MVP"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
news-study = "app.main:main"
news-study-rss = "app.rss_collector:main"

[tool.ruff]
line-length = 100
